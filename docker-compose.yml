services:
  playwright:
    image: mcr.microsoft.com/playwright:v1.59.1-jammy
    volumes:
      - .:/app
    working_dir: /app
    command: npx playwright test
    environment:
      - NODE_ENV=test
    ipc: host
